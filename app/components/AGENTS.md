# Components - Agents

- Names are primary-first: `Button`, `ButtonOutline`, `Dropdown`, `DropdownMenu`.
- Use shadcn components first; avoid wrapper components unless they add real behavior.
- Prefer slots / provide-inject / composables over prop drilling.
- Keep props minimal, typed, and validated only once.
