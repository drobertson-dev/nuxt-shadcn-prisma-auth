# Shared - Agents

- Shared code only: types, constants, pure helpers, validation schemas.
- No Vue reactivity or browser-only APIs.

## Auto-imports

- `shared/utils/**` and `shared/types/**` are auto-imported.
- Other folders/files are not.

## Importing

- `#shared/capitalize`
- `#shared/formatters/lower`
- `#shared/utils/formatters/upper`
